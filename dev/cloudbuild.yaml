steps:  
- id: 'tf init and tf plan'
  name: 'hashicorp/terraform:0.14.9'
  entrypoint: 'sh'
  args: 
  - '-c'
  - |
      if [ $_ENV_TYPE != "PUSH" ]; then
        git clone $_HEAD_REPO_URL
        cd $REPO_NAME
        git checkout $_HEAD_BRANCH
        git diff --quiet $_HEAD_BRANCH remotes/origin/$_BASE_BRANCH -- $_CHECK_DIR_PATH || echo true > output.log
        if grep -q "true" output.log; then
          echo "inside loop"
          cd dev/terraform
          terraform init
          terraform plan
        fi
      fi

- id: 'tf init, tf plan and tf apply'
  name: 'hashicorp/terraform:0.14.9'
  entrypoint: 'sh'
  args: 
  - '-c'
  - |
      if [ $_ENV_TYPE == "PUSH" ]; then
        cd dev/terraform
        terraform init
        terraform plan
        terraform apply -auto-approve
      fi
