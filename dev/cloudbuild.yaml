steps:  
- id: 'tf init'
  name: 'hashicorp/terraform:0.14.9'
  entrypoint: 'sh'
  args: 
  - '-c'
  - |
      cd dev
      check_status=$(git diff --quiet HEAD master -- terraform || echo true)
      echo $BRANCH_NAME
      echo $check_status
      if [[ $check_status ]]
      then
        echo "inside init"
        cd terraform
        terraform init
      fi

- id: 'tf plan'
  name: 'hashicorp/terraform:0.14.9'
  entrypoint: 'sh'
  args: 
  - '-c'
  - |
      cd dev
      check_status=$(git diff --quiet HEAD master -- terraform || echo true)
      echo $BRANCH_NAME
      echo $check_status
      if [[ $check_status ]]
      then
        echo "inside plan"
        terraform plan
      fi

- id: 'tf apply'
  name: 'hashicorp/terraform:0.14.9'
  entrypoint: 'sh'
  args: 
  - '-c'
  - |
      if [ $_ENV_TYPE == "PUSH" ]; then
        cd dev/terraform
        terraform apply -auto-approve
      fi
